services:
  opencode-chamber:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - "./opencode-config:/home/agent/.config/opencode"
      - "./codes:/home/agent/repositories"
    environment:
      - UI_PASSWORD=
      - OPENCHAMBER_HOST=0.0.0.0
    restart: unless-stopped
