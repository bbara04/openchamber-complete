services:
  opencode-chamber:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - "./opencode-config:/root/.config/opencode"
      - "./repositories:/root/repositories"
    environment:
      - UI_PASSWORD=Titok123
      - OPENCHAMBER_HOST=0.0.0.0
    restart: unless-stopped
    network_mode: host
